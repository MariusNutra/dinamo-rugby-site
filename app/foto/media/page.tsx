import MediaClient from './MediaClient'

export const metadata = {
  title: 'Portal Foto | Dinamo Rugby Juniori',
  robots: { index: false, follow: false },
}

export default function FotoMediaPage() {
  return <MediaClient />
}
